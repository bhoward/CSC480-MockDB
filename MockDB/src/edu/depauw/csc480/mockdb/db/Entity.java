package edu.depauw.csc480.mockdb.db;

public interface Entity {
	// TODO add a mechanism so that entities may be persisted to a database,
	// or perhaps emitted as SQL or CSV for import into a database
}
